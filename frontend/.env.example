# Base URL of the API gateway used when the SPA is served via Vite (development)
VITE_API_BASE=https://localhost:3000

# Internal API key forwarded to the gateway for privileged routes (e.g. user login)
# Replace the placeholder below with the value generated in your root .env file.
VITE_INTERNAL_API_KEY=change-me
